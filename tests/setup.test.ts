import { describe, it, expect } from 'vitest';

describe('ambiente de testes', () => {
  it('deve_confirmar_que_o_vitest_esta_configurado_corretamente', () => {
    expect(true).toBe(true);
  });
});
